{
  "name": "deepenv",
  "version": "1.1.3",
  "description": "Takes environment variables and converts into a deeply nested configuration object based on a customisable delimiter (__) to represent a level of nesting in the config json (.) ",
  "keywords": [
    "environment variables",
    "object",
    "configuration"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/renewooller/deepenv.git"
  },
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "lodash.set": "^4.3.2"
  },
  "devDependencies": {
    "jest": "^27.4.5"
  }
}
